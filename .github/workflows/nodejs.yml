name: tests
run-name: testing in node
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: npm install, build
        run: |
          npm install
          npm run build --if-present
          npm run lint
        env:
          CI: true
      - name: Build tests
        run: |
          npm run build-tests
      - name: install xvfb
        run: sudo apt-get install xvfb firefox
      - name: Run tests
        run: |
          xvfb-run --auto-servernum npm run test-tape-run
